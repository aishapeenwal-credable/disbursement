services:
  - type: web
    name: ocr-api
    env: python
    buildCommand: ./build.sh
    startCommand: uvicorn disbursement:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: TOGETHER_API_KEY
        sync: false
